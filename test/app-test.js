var app = require("../lib/app.js");
/*

describe("App", function() {
	describe("#init()", function() {
		it("Should emmit object with expected properties", function(done) {
			
			app.on("update", function(bar) {
				expect(bar).to.have.property("symbolName", "dateTime", "open", "high", "low", "close", "volume");
				done();
			});
			
		});
	});
});

describe('Testing', function(){
  it('Should send an object with a testing key', function(done){
    res.on('response', function(resp) {
      expect(resp.response).to.have.property('testing');
      expect(resp.response.testing).to.equal(true);
      done();
    });
    
    testing.test({}, res);
  });
});
*/